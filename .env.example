# ============================================
# Configuração da Aplicação Web - Consulta de Boletos Cora
# ============================================
# 
# Copie este arquivo para .env e ajuste os valores conforme necessário
# cp .env.example .env
#

# ============================================
# Configurações do Servidor Flask
# ============================================

# Porta do servidor (padrão: 5000)
PORT=5000

# Host do servidor (padrão: 0.0.0.0 para aceitar conexões de qualquer IP)
# Use 127.0.0.1 para aceitar apenas conexões locais
HOST=0.0.0.0

# Modo debug (true/false)
# ATENÇÃO: Em produção, sempre use false
DEBUG=false

# Chave secreta do Flask (obrigatório em produção)
# Gere uma chave segura com: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-secret-key-in-production

# ============================================
# Configurações da API Cora
# ============================================

# Arquivo de configuração YAML da API (padrão: config.yaml)
# Este arquivo deve conter as configurações da API do Cora (auth_url, base_url, etc.)
CONFIG_FILE=config.yaml

# ============================================
# Configurações de Logging (opcional)
# ============================================

# Nível de logging (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# Arquivo de log (opcional)
# LOG_FILE=app.log
